{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Audio DSP Project",
            "type": "shell",
            "command": "${workspaceFolder}/build.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$msCompile"],
            "detail": "Build all cpp files using MSVC x64 and PortAudio, works in PowerShell"
        }
    ]
}
